services:
  - type: web
    name: connected-healthcare
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run connected_healthcare_redesigned/connected_healthcare/app.py --server.port $PORT --server.address 0.0.0.0
